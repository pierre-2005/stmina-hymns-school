services:
  web:
    build: .
    container_name: stminahs-web
    environment:
      TZ: America/Edmonton
      CONTENT_PATH: /app/content/site.xlsx
    volumes:
      - ./content:/app/content:ro
    ports:
      - "127.0.0.1:8090:8000"
    restart: unless-stopped
